# lease-plan (NL)

## Current status
_Needs manual triage — see history below and update this line when you know the current state._

## History & quirks (newest first where known)
- `EPROTO` — migrated. No action.
- `engineCapacity` out-of-range 77.6.
- Redirects to `usedcars.ayvens.com` (Oct 2024). LeasePlan weekly URL-check reminder (every Tue 9AM) was set up to monitor this — the site name might come back under ayvens brand if they re-add inventory.

## Related patterns
_Cross-referenced in failure-patterns.md. Grep that file for this site's name to find them._

---

<!-- Maintenance:
When you add a new entry, put it at the TOP of the history section with a date.
Use format: **YYYY-MM-DD** — what happened + outcome.
When a site is disabled or an issue is resolved, update the "Current status" line.
-->
